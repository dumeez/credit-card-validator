{
  "name": "credit-card-validator",
  "version": "1.0.0",
  "description": "Check the validity and return the credit card's type (Visa, MasterCard, Maestro...)",
  "main": "src/index.js",
  "scripts": {
    "postinstall": "mkdir dist && cd ./dist && touch index.js && browserify src/index.js > dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dumeez/credit-card-validator.git"
  },
  "keywords": [
    "Credit",
    "Card",
    "Number",
    "Validator",
    "Visa",
    "MasterCard",
    "Maestro"
  ],
  "author": "Yestudent",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.1.1"
  }
}
